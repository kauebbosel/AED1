#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void le_nome(char *nome);

int main(){
    char strTemp[200];
    int menu=0;
    
    do{
        printf("1.Adicionar nome 2.Remover nome 3.Listar. 4.Sair\n");
        scanf("%d", &menu);

        if(menu == 1){
            printf("Formato:Nome Sobrenome_\n");
            le_nome(strTemp);

            
/*			for(int i=0; i< tamanhoNome; i++){
				if(strTemp[i] != '_'){
					//listaNomes[i] = strTemp[i];
				}else{
				listaNomes[i] = ' ';
				}
				
			}*/
        }
        if(menu == 2){
			

        }

        if(menu == 3){
//			printf("%s\n", listaNomes);
        }

        if(menu == 4){
            printf("Saindo\n");
        }

    } while(menu != 4);
}

void le_nome(char *nome){
	

}
